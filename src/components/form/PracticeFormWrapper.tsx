import styled from 'styled-components';

const PracticeFormWrapper = styled.div`
  display: flex;
  flex-direction: column;
  width: 100%;
  //position: relative;
`;

export default PracticeFormWrapper;
