import styled from 'styled-components';

const StyledButton = styled.button`
  padding: 12px 24px;
  max-width: fit-content;

  background-color: rgb(239, 151, 92);
  border-radius: 6px;
`;

export default StyledButton;
